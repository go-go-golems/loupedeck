# loupedeck xgoja provider smoke

Builds a generated xgoja binary that imports the public `pkg/xgoja/provider` wrapper, selects the safe `loupedeck/easing` and `loupedeck/gfx` modules, and runs a deterministic smoke script.

```bash
make smoke
```
